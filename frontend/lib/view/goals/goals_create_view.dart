import 'package:flutter/material.dart';

class GoalsCreateView extends StatelessWidget {
  const GoalsCreateView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Settings Page', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
