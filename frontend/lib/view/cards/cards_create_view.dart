import 'package:flutter/material.dart';

class CardsCreateView extends StatelessWidget {
  const CardsCreateView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Settings Page', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
