import 'package:flutter/material.dart';

class RegularPaymentsView extends StatelessWidget {
  const RegularPaymentsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Regular Payments Page', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
